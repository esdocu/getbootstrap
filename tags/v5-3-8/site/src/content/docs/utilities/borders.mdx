---
title: Bordes (Borders)
description: Utiliza las utilidades de borde para dar estilo rápidamente al borde y al radio de borde (border-radius) de un elemento. Excelente para imágenes, botones o cualquier otro elemento.
toc: true
---

import { getData } from '@libs/data'

## Borde (Border)

Utiliza las utilidades de borde para agregar o quitar los bordes de un elemento. Elige entre todos los bordes o uno a la vez.

### Aditivo

Agregar bordes a elementos personalizados:

<Example class="bd-example-border-utils" code={`<span class="border"></span>
<span class="border-top"></span>
<span class="border-end"></span>
<span class="border-bottom"></span>
<span class="border-start"></span>`} />

### Sustractivo

O eliminar bordes:

<Example class="bd-example-border-utils" code={`<span class="border border-0"></span>
<span class="border border-top-0"></span>
<span class="border border-end-0"></span>
<span class="border border-bottom-0"></span>
<span class="border border-start-0"></span>`} />

## Color

<Callout>
Las utilidades de borde como `.border-*` que se generan a partir de nuestro mapa Sass original `$theme-colors` aún no responden a los modos de color, sin embargo, cualquier utilidad `.border-*-subtle` sí lo hará. Esto se resolverá en la versión 6.
</Callout>

Cambia el color del borde utilizando utilidades basadas en los colores de nuestro tema.

<Example class="bd-example-border-utils" code={[
  ...getData('theme-colors').map((themeColor) => `<span class="border border-${themeColor.name}"></span>
<span class="border border-${themeColor.name}-subtle"></span>`),
  `<span class="border border-black"></span>
<span class="border border-white"></span>`
]} />

Or modify the default `border-color` of a component:

<Example code={`<div class="mb-4">
    <label for="exampleFormControlInput1" class="form-label">Dirección de correo electrónico</label>
    <input type="email" class="form-control border-success" id="exampleFormControlInput1" placeholder="name@example.com">
  </div>

  <div class="h4 pb-2 mb-4 text-danger border-bottom border-danger">
    Encabezado peligroso
  </div>

  <div class="p-3 bg-info bg-opacity-10 border border-info border-start-0 rounded-end">
    Cambiar el color y el ancho del borde
  </div>`} />

## Opacidad (Opacity)

<AddedIn version="5.2.0" />

Las utilidades `border-{color}` de Bootstrap se generan con Sass utilizando variables CSS. Esto permite cambios de color en tiempo real sin compilación y cambios dinámicos de transparencia alfa.

### Cómo funciona

Considera nuestra utilidad `.border-success` predeterminada.

```css
.border-success {
  --bs-border-opacity: 1;
  border-color: rgba(var(--bs-success-rgb), var(--bs-border-opacity)) !important;
}
```

Usamos una versión RGB de nuestra variable CSS `--bs-success` (con el valor de `25, 135, 84`) y adjuntamos una segunda variable CSS, `--bs-border-opacity`, para la transparencia alfa (con un valor predeterminado `1` gracias a una variable CSS local). Eso significa que cada vez que uses `.border-success` ahora, tu valor de color (`color`) calculado será `rgba(25, 135, 84, 1)`. La variable CSS local dentro de cada clase `.border-*` evita problemas de herencia para que las instancias anidadas de las utilidades no tengan automáticamente una transparencia alfa modificada.

### Ejemplo

Para cambiar esa opacidad, sobrescribe `--bs-border-opacity` a través de estilos personalizados o estilos en línea.

<Example code={`<div class="border border-success p-2 mb-2">Este es el borde success predeterminado</div>
<div class="border border-success p-2" style="--bs-border-opacity: .5;">Este es el borde success con 50% de opacidad</div>`} />

O bien, elige entre cualquiera de las utilidades `.border-opacity`:

<Example code={`<div class="border border-success p-2 mb-2">Este es el borde success predeterminado</div>
<div class="border border-success p-2 mb-2 border-opacity-75">Este es el borde success con 75% de opacidad</div>
<div class="border border-success p-2 mb-2 border-opacity-50">Este es el borde success con 50% de opacidad</div>
<div class="border border-success p-2 mb-2 border-opacity-25">Este es el borde success con 25% de opacidad</div>
<div class="border border-success p-2 border-opacity-10">Este es el borde success con 10% de opacidad</div>`} />

## Ancho (Width)

<Example class="bd-example-border-utils" code={`<span class="border border-1"></span>
<span class="border border-2"></span>
<span class="border border-3"></span>
<span class="border border-4"></span>
<span class="border border-5"></span>`} />

## Radio (Radius)

Agrega clases a un elemento para redondear fácilmente sus esquinas.

<Example class="bd-example-rounded-utils" code={`<Placeholder width="75" height="75" class="rounded" title="Ejemplo de imagen redondeada" />
<Placeholder width="75" height="75" class="rounded-top" title="Ejemplo de imagen redondeada en la parte superior" />
<Placeholder width="75" height="75" class="rounded-end" title="Ejemplo de imagen redondeada a la derecha" />
<Placeholder width="75" height="75" class="rounded-bottom" title="Ejemplo de imagen redondeada en la parte inferior" />
<Placeholder width="75" height="75" class="rounded-start" title="Ejemplo de imagen redondeada a la izquierda" />`} />

### Tamaños

Usa las clases de escala para esquinas redondeadas más grandes o más pequeñas. Los tamaños varían de `0` a `5`, incluidos `circle` y `pill`, y se pueden configurar modificando la API de utilidades.

<Example class="bd-example-rounded-utils" code={`<Placeholder width="75" height="75" class="rounded-0" title="Ejemplo de imagen no redondeada" />
<Placeholder width="75" height="75" class="rounded-1" title="Ejemplo de imagen redondeada pequeña" />
<Placeholder width="75" height="75" class="rounded-2" title="Ejemplo de imagen redondeada predeterminada" />
<Placeholder width="75" height="75" class="rounded-3" title="Ejemplo de imagen redondeada grande" />
<Placeholder width="75" height="75" class="rounded-4" title="Ejemplo de imagen redondeada más grande" />
<Placeholder width="75" height="75" class="rounded-5" title="Ejemplo de imagen redondeada extra grande" />
<Placeholder width="75" height="75" class="rounded-circle" title="Imagen completamente redonda" />
<Placeholder width="150" height="75" class="rounded-pill" title="Imagen de píldora redondeada" />`} />

<Example class="bd-example-rounded-utils" code={`<Placeholder width="75" height="75" class="rounded-bottom-1" title="Ejemplo de imagen redondeada pequeña" />
<Placeholder width="75" height="75" class="rounded-start-2" title="Ejemplo de imagen redondeada a la izquierda predeterminada" />
<Placeholder width="75" height="75" class="rounded-end-circle" title="Ejemplo de imagen completamente redonda a la derecha" />
<Placeholder width="75" height="75" class="rounded-start-pill" title="Ejemplo de imagen de píldora redondeada a la izquierda" />
<Placeholder width="75" height="75" class="rounded-5 rounded-top-0" title="Ejemplo de imagen redondeada en la parte inferior extra grande" />`} />

## CSS

### Variables

<AddedIn version="5.2.0" />

<ScssDocs name="root-border-var" file="scss/_root.scss" />

### Variables Sass

<ScssDocs name="border-variables" file="scss/_variables.scss" />

<ScssDocs name="border-radius-variables" file="scss/_variables.scss" />

Variables para establecer `border-color` en utilidades `.border-*-subtle` en modo claro y oscuro:

<ScssDocs name="theme-border-subtle-variables" file="scss/_variables.scss" />

<ScssDocs name="theme-border-subtle-dark-variables" file="scss/_variables-dark.scss" />

### Mapas Sass

Los colores de borde adaptables al modo de color también están disponibles como un mapa Sass:

<ScssDocs name="theme-border-subtle-map" file="scss/_maps.scss" />

<ScssDocs name="theme-border-subtle-dark-map" file="scss/_maps.scss" />

### Mixins Sass

<ScssDocs name="border-radius-mixins" file="scss/mixins/_border-radius.scss" />

### API de utilidades de Sass

Las utilidades de borde se declaran en nuestra API de utilidades en `scss/_utilities.scss`. [Aprende a utilizar la API de utilidades.]([[docsref:/utilities/api#using-the-api]])

<ScssDocs name="utils-borders" file="scss/_utilities.scss" />

<ScssDocs name="utils-border-radius" file="scss/_utilities.scss" />
