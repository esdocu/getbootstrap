---
title: Selección (Select)
description: Personaliza los selectores `<select>` nativos con CSS personalizado que cambia la apariencia inicial del elemento.
toc: true
---

## Predeterminado

Los menús `<select>` personalizados solo necesitan una clase personalizada, `.form-select`, para activar los estilos personalizados. Los estilos personalizados se limitan a la apariencia inicial de `<select>` y no pueden modificar las opciones `<option>` debido a limitaciones del navegador.

<Example code={`<select class="form-select" aria-label="Ejemplo de selección predeterminado">
    <option selected>Abre este menú de selección</option>
    <option value="1">Uno</option>
    <option value="2">Dos</option>
    <option value="3">Tres</option>
  </select>`} />

## Tamaño

También puedes elegir entre selectores personalizados grandes y pequeños para que coincidan con nuestras entradas de texto de tamaño similar.

<Example code={`<select class="form-select form-select-lg mb-3" aria-label="Ejemplo de selección grande">
    <option selected>Abre este menú de selección</option>
    <option value="1">Uno</option>
    <option value="2">Dos</option>
    <option value="3">Tres</option>
  </select>

  <select class="form-select form-select-sm" aria-label="Ejemplo de selección pequeña">
    <option selected>Abre este menú de selección</option>
    <option value="1">Uno</option>
    <option value="2">Dos</option>
    <option value="3">Tres</option>
  </select>`} />

También se admite el atributo `multiple`:

<Example code={`<select class="form-select" multiple aria-label="Ejemplo de selección múltiple">
    <option selected>Abre este menú de selección</option>
    <option value="1">Uno</option>
    <option value="2">Dos</option>
    <option value="3">Tres</option>
  </select>`} />

Al igual que el atributo `size`:

<Example code={`<select class="form-select" size="3" aria-label="Ejemplo de selección de tamaño 3">
    <option selected>Abre este menú de selección</option>
    <option value="1">Uno</option>
    <option value="2">Dos</option>
    <option value="3">Tres</option>
  </select>`} />

## Deshabilitado

Agrega el atributo booleano `disabled` en un elemento de selección para darle una apariencia atenuada y eliminar eventos de puntero.

<Example code={`<select class="form-select" aria-label="Ejemplo de selección deshabilitada" disabled>
    <option selected>Abre este menú de selección</option>
    <option value="1">Uno</option>
    <option value="2">Dos</option>
    <option value="3">Tres</option>
  </select>`} />

## CSS

### Variables Sass

<ScssDocs name="form-select-variables" file="scss/_variables.scss" />
