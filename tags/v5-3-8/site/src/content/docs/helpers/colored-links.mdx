---
title: Enlaces coloreados (Colored links)
description: Enlaces coloreados con estados de desplazamiento (hover).
toc: true
---

import { getData } from '@libs/data'

## Colores de enlaces

Puedes utilizar las clases `.link-*` para colorear enlaces. A diferencia de las [clases `.text-*`]([[docsref:/utilities/colors]]), estas clases tienen un estado `:hover` y `:focus`. Algunos de los estilos de enlaces utilizan un color de primer plano relativamente claro y solo deben usarse sobre un fondo oscuro para tener suficiente contraste.

<Callout>
**¡Atención!** `.link-body-emphasis` es actualmente el único enlace coloreado que se adapta a los modos de color. Se trata como un caso especial hasta que llegue la versión 6 y podamos reconstruir más a fondo los colores de nuestro tema para los modos de color. Hasta entonces, es un color de enlace único y de alto contraste con estilos `:hover` y `:focus` personalizados. Sin embargo, sigue respondiendo a las nuevas utilidades de enlaces.
</Callout>

<Example code={[
  ...getData('theme-colors').map((themeColor) => `<p><a href="#" class="link-${themeColor.name}">Enlace ${themeColor.title}</a></p>`),
  `<p><a href="#" class="link-body-emphasis">Enlace destacado (Emphasis link)</a></p>`
]} />

<Callout name="warning-color-assistive-technologies" />

## Utilidades de enlaces

<AddedIn version="5.3.0"/>

Los enlaces coloreados también se pueden modificar mediante nuestras [utilidades de enlaces]([[docsref:/utilities/link/]]).

<Example code={[
  ...getData('theme-colors').map((themeColor) => `<p><a href="#" class="link-${themeColor.name} link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover">Enlace ${themeColor.title}</a></p>`),
  `<p><a href="#" class="link-body-emphasis link-offset-2 link-underline-opacity-25 link-underline-opacity-75-hover">Enlace destacado</a></p>`
]} />
